1.  docker compose up -d --build

2.  docker compose exec nginx ping database -> Nos va a decir que no encuentra
											   database porque no están en la misma red

3.  docker compose exec nginx ping api -> Devuelve el ping correctamente porque el 
										  frontend se puede contectar con el backend(api)

